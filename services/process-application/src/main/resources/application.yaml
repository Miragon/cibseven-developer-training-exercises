# TODO Exercise 1: Uncomment the configuration.
#
# It connects the engine to the PostgreSQL from stack/ (docker-compose), creates the
# Cockpit admin user and configures the web client (among other things the JWT secret for the Cockpit login).
# Without this configuration the engine finds no database and the application does not start.
#
# The engine runs on port 8080 with its own schema `exercise`.

server:
  port: 8080

#spring:
#  datasource:
#    url: jdbc:postgresql://localhost:5432/cibseven-training?currentSchema=exercise
#    username: admin
#    password: admin
#    driver-class-name: org.postgresql.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
#        default_schema: exercise
#
#camunda:
#  bpm:
#    admin-user:
#      id: admin
#      password: admin
#    database:
#      type: postgres
#      schema-update: true
#
#cibseven:
#  webclient:
#    services:
#      basePath: services/v1
#    engineRest:
#      url: http://localhost:8080
#    authentication:
#      jwtSecret: M9nU3ORo3s+gK23D9mO5I2h+EIqnosCFDCJi+2bKoulKqZkeQT8pGYg5RhuORlf/fWhLu5meC/SPZCv9NNuj6SK/vE5Sid04UQGrnyh04EpBdiAosAO91xezjgmbSeALUtneibseGpS0tNE4RvLIl+gXiAKqNXyO
