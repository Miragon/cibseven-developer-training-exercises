{
  "name": "cibseven-developer-training-exercises-tooling",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling for the CIB seven training exercises: BPMN linting (bpmnlint:recommended). Run from the repo root via `npm --prefix tools run <script>`.",
  "scripts": {
    "lint:bpmn": "bpmnlint \"../services/**/src/main/resources/bpmn/*.bpmn\" \"../templates/**/src/main/resources/bpmn/*.bpmn\""
  },
  "devDependencies": {
    "bpmnlint": "11.12.1"
  }
}
